import { combineReducers } from 'redux';

// Register your redux store under a unique namespace
export const namespace = 'external-phone-ani-overwrite';

// Combine the reducers
export default combineReducers({
});
